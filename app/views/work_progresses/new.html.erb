<%= form_with(model:[ :project, @work_progress]) do |f| %>
  <%= f.hidden_field :supplier, value: params[:supplier][:supplier_id] %>
  <%= f.hidden_field :total, value: 0 %>
  <%= f.submit "Confirmar" %>
<% end %>